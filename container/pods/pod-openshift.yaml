# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#
# Created with podman-5.1.1
apiVersion: v1
kind: Pod
metadata:
  labels:
    app: domain-mode-demo-ocp
  # This is the hostname, used to connect from host controller to domain mode
  name: domain-mode-demo-ocp
spec:
  containers:
  - env:
    - name: TERM
      value: xterm
    - name: JBOSS_EAP_DOMAIN_HOST_NAME
      value: primary
    - name: JAVA_MAX_MEM_RATIO
      value: 25
    - name: SERVER_ARGS
      value: --host-config=host-primary.xml
    image: quay.io/jdenise/my-domain-controller:latest
    name: domain-controller-container
    ports:
    - containerPort: 9990
      hostIP: 0.0.0.0
      hostPort: 9990
    securityContext:
      runAsNonRoot: true
  - env:
    - name: TERM
      value: xterm
    - name: SERVER_ARGS
      value: --host-config=my-host.xml -Djboss.domain.primary.address=domain-mode-demo-ocp -Djboss.management.http.port=9991
    - name: JBOSS_EAP_DOMAIN_HOST_NAME
      value: primary
    - name: JAVA_MAX_MEM_RATIO
      value: 25
    image: quay.io/jdenise/my-host-controller:1.0
    name: secondary-host-controller-container
    ports:
    - containerPort: 8080
      hostIP: 0.0.0.0
      hostPort: 8080
    securityContext:
      runAsNonRoot: true

